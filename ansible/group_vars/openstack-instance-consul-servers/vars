openstack_instance_security_groups:
 - "{{ (groups['openstack-security-groups-ssh'] | map('extract', hostvars) | selectattr('tenant', 'equalto', tenant) | list())[0].openstack_security_group_name }}"
 - "{{ (groups['openstack-security-groups-consul-server'] | map('extract', hostvars) | selectattr('tenant', 'equalto', tenant) | list())[0].openstack_security_group_name }}"

