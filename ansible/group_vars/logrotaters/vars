---

logrotate_conf_dir: /etc/logrotate.d/

logrotate_scripts:
  - name: syslog
    path: /var/log/syslog
    options:
      - daily
      - copytruncate
      - maxsize 1G
      - missingok
      - compress
      - rotate 1

logrotate_replace_old_configurations: true
