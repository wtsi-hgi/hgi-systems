# Vault contains entries like this:
#openstack_credentials_logins:
#  cluster-username:
#    auth_url: xxx
#    username: xxx
#    password: xxx

openstack_credentials_tenant_auths:
  delta-hgi-ci: "{{ openstack_credentials_logins['delta-hgi-ci'] | combine({'project_name': 'hgi-ci'}) }}"
  delta-hgi: "{{ openstack_credentials_logins['delta-jr17'] | combine({'project_name': 'hgi'}) }}"
  gamma-hgi: "{{ openstack_credentials_logins['gamma-jr17'] | combine({'project_name': 'hgi'}) }}"
