openstack_instance_image: "{{ hostvars[image]['openstack_name'] }}"
openstack_instance_key_name: "{{ hostvars[keypair]['openstack_name'] }}"
openstack_instance_volumes: "{{ [ hostvars[volume]['openstack_name'] ] if volume is defined else [] }}"
openstack_instance_network: "{{ (groups[netgroup] | map('extract', hostvars) | selectattr('openstack_type', 'equalto', 'network') | list())[0].openstack_name }}"
openstack_instance_subnet: "{{ (groups[netgroup] | map('extract', hostvars) | selectattr('openstack_type', 'equalto', 'subnet') | list())[0].openstack_name }}"
openstack_instance_router: "{{ (groups[netgroup] | map('extract', hostvars) | selectattr('openstack_type', 'equalto', 'router') | list())[0].openstack_name }}"

openstack_instance_infoblox_username: "{{ infoblox_credentials_username }}"
openstack_instance_infoblox_password: "{{ infoblox_credentials_password }}"
openstack_instance_infoblox_api_server: "{{ infoblox_credentials_api_server }}"
openstack_instance_infoblox_api_version: "{{ infoblox_credentials_api_version }}"

openstack_instance_dns_ttl: 60

openstack_instance_ssh_user: ubuntu
