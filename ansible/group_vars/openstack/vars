# Vault contains entries like this:
#openstack_logins:
#  cluster-username:
#    auth_url: xxx
#    username: xxx
#    password: xxx

openstack_tenant_auths:
  delta-hgi-ci-tenant: "{{ openstack_logins['delta-mercury'] | combine({'project_name': 'hgi-ci'}) }}"
  delta-hgi-tenant: "{{ openstack_logins['delta-mercury'] | combine({'project_name': 'hgi'}) }}"

openstack_auth: "{{ openstack_tenant_auths[tenant] }}"
